<!DOCTYPE html>
<html>
    <head>
        <title>{{title}}</title>
        <link rel="stylesheet" href="/css/styles.css">
    </head>
    <body>
        <div class='mainContent'> {{!you can type div.mainContent and hit tab instead as a shorthand}}
            {{>header}}
            <p>
                {{notFoundText}}
            </p>
        </div>
        {{>footer}}
    </body>
</html>
